// Toggle to force the Splash screen to stay visible, regardless of real
// auth-loading state. Handy for tweaking Splash styles without waiting
// for/re-triggering an actual cold-start session check.
// ⚠️ Always set back to false before committing/building.
export const DEV_FORCE_SPLASH = false;